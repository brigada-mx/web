server {
  listen 8080;
  server_name 919.local.mx;
  set $root /Users/kylebebak/GoogleDrive/Code/Work/919/web;

  location / {
    root $root/html;
    index index.html index.htm;
  }

  location /styles/ {
    root $root;
  }

  location /assets/ {
    root $root;
  }

  location /mapa {
    rewrite ^/mapa/$ /map.html;
  }
}
