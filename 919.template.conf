server {
  # duplicate me as 919.conf and
  set $root </project/root>;

  listen 8080;
  server_name 919.local.mx;

  location / {
    root $root/html;
    index index.html index.htm;
    rewrite ^/mapa$ /map.html;
    rewrite ^/mapa/$ /map.html;
  }

  location /styles/ {
    root $root;
  }

  location /assets/ {
    root $root;
  }
}
